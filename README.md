# IntelliComplete
A simple but smart word prediction system using Trie logic.

I have implemented this basic logic to showcase how word suggestions can be made much faster using this algorithm instead of the basic linear search which is very slow when the word library is large.